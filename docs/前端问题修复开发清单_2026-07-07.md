# 秘仪室前端问题修复开发清单（2026-07-07）

## 1. 文档目的

本清单基于 [前端联调问题清单_2026-07-05](</Users/yuhui/Documents/coc kp platform/docs/前端联调问题清单_2026-07-05.md>) 整理，目的是把“已复现问题”和“可精进项”转成可以直接执行、直接勾选的开发任务。

本阶段不再讨论 React 重构底座本身，而是聚焦修复主链路与补回归保障。

## 2. 本阶段目标

- 恢复内置模板房间的 KP -> 服务端 -> 玩家端同步主链路
- 补齐玩家端缺失的“在场玩家”展示
- 修正玩家端移动端首屏遮挡问题
- 明确 KP 专用素材与玩家可见素材边界
- 为关键链路补上自动化回归

## 2.1 本次拆分后的执行粒度

为避免任务仍然过粗，本文统一按下面的粒度拆分：

- 一级任务：阶段目标，例如 `A-1`
- 二级任务：可以单独提交、单独验证的开发动作，例如 `A-1.2`
- 每个二级任务都必须包含明确产出物和至少一条验证动作

## 2.2 优先级总览

| 编号 | 主题 | 优先级 | 前置依赖 | 建议提交粒度 |
| --- | --- | --- | --- | --- |
| A-1 | 内置模板完整解析 | P0 | 无 | 1 次独立提交 |
| A-2 | 内置模板状态推送修复 | P0 | A-1 | 1 次独立提交 |
| A-3 | 同步失败用户态兜底 | P1 | A-2 | 1 次独立提交 |
| B-1 | 玩家席位展示能力 | P1 | A-2 | 1 次独立提交 |
| C-1 | 房间状态徽章避让 | P1 | B-1 | 可与 C-2 合并提交 |
| B-2 | KP 专用素材边界澄清 | P2 | A-2 | 可与 C-2 合并提交 |
| C-2 | 首屏信息优先级整理 | P2 | C-1 | 可与 C-1 合并提交 |
| D-1 | 内置模板同步 smoke 用例 | P1 | A-2 | 1 次独立提交 |
| D-2 | KP -> 玩家端最小联调用例 | P1 | B-1、A-3 | 可与 D-1 合并提交 |

## 3. 推进原则

### 3.1 先修主阻塞，再修表现层

执行顺序固定为：

1. 修复内置模板同步
2. 收口同步失败提示
3. 补玩家端玩家席位展示
4. 修移动端遮挡与首屏信息层级
5. 补自动化回归

### 3.2 问题清单保留，任务清单承接

- 问题原因、复现步骤、现象描述继续保留在问题清单
- 本文只写开发动作、交付物、验收标准

### 3.3 每个阶段都要可回归验证

每完成一个阶段，至少补一条人工验证或自动化验证，避免“接口修了，但真实联动仍然坏着”。

## 4. 阶段 A：主链路恢复

### A-1 内置模板完整解析

目标：服务端在处理房间状态同步时，始终基于“完整模板对象”做校验，而不是基于默认值推断。

拆分任务：

- [ ] `A-1.1` 盘点当前建房、读房、写状态三条链路里模板数据的来源
- [ ] `A-1.2` 为服务端增加模板 ID -> 完整模板对象的统一解析入口
- [ ] `A-1.3` 停止在状态同步校验中直接依赖 `templateDefaults(...)`
- [ ] `A-1.4` 明确 `locations`、`npcs`、`materials`、`players` 的服务端来源并统一命名
- [ ] `A-1.5` 确认空白房间、内置模板房间、自定义导入模板房间都走兼容分支

产出物：

- 一处统一模板解析函数或模块入口
- 一处统一的“房间可选内容集合”读取来源
- 一次覆盖三种建房来源的人工验证记录

涉及文件：

- [server.mjs](/Users/yuhui/Documents/coc kp platform/server.mjs:495)
- [src/shared/templates/index.ts](/Users/yuhui/Documents/coc kp platform/src/shared/templates/index.ts:1)
- [src/shared/room/state.ts](/Users/yuhui/Documents/coc kp platform/src/shared/room/state.ts:1)

验证动作：

- [ ] 分别创建空白房间、默认模组房间、导入模板房间
- [ ] 记录三种房间的模板解析结果是否都包含完整 `locations / npcs / materials / players`

### A-2 内置模板状态推送修复

目标：默认模组房间创建后，KP 对舞台内容的每一类切换都能稳定写入并回读。

拆分任务：

- [ ] `A-2.1` 修复内置模板房间选择 `locationId` 时的合法性校验
- [ ] `A-2.2` 修复内置模板房间选择 `npcId` 时的合法性校验
- [ ] `A-2.3` 修复内置模板房间选择 `materialId` 时的合法性校验
- [ ] `A-2.4` 修复内置模板房间选择 `playerIds` 时的合法性校验
- [ ] `A-2.5` 确认服务端状态写回后可重新读取，且 SSE 推送内容一致
- [ ] `A-2.6` 补一条最小日志或调试输出，便于再次定位模板同步失败点

产出物：

- 一次默认模组房间的完整切换验证
- 一次服务端状态写入前后字段对照
- 一次玩家端刷新回读验证

验收标准：

- 创建“雾中献血车”房间后，KP 切换地点 / NPC / 素材 / 玩家席位均返回 `200`
- KP 顶部状态不再出现原始异常
- 玩家端刷新后能看到最新状态

验证动作：

- [ ] 顺序执行地点 -> NPC -> 素材 -> 玩家席位切换
- [ ] 每一步都确认 `POST /api/rooms/:room/state` 返回 `200`
- [ ] 每一步都确认刷新玩家端后仍保持最新值

### A-3 同步失败用户态兜底

目标：同步失败时，KP 看见的是可理解、可操作的提示，而不是直接暴露底层异常。

拆分任务：

- [ ] `A-3.1` 将用户可见错误文案与底层异常对象分离
- [ ] `A-3.2` 为同步失败提供统一用户提示入口
- [ ] `A-3.3` 保留原始异常到控制台或日志链路
- [ ] `A-3.4` 区分“可重试失败”与“模板数据异常”两类提示
- [ ] `A-3.5` 明确提示文案对应的恢复动作，例如“重试同步”或“重新建房”

涉及文件：

- [src/shared/room/useKpRoom.ts](/Users/yuhui/Documents/coc kp platform/src/shared/room/useKpRoom.ts:1)

验收标准：

- 人工制造一次同步失败时，页面不直接显示 JS 原始报错

验证动作：

- [ ] 模拟一次网络失败或服务端 500
- [ ] 模拟一次模板字段不合法
- [ ] 确认两类异常的用户提示不同，但控制台仍保留原始错误

## 5. 阶段 B：玩家端可见性补齐

### B-1 玩家席位展示能力

目标：玩家端能稳定看到当前在场玩家，而不是只有 KP 预览可见。

拆分任务：

- [ ] `B-1.1` 为玩家端补齐在场玩家展示区
- [ ] `B-1.2` 复用 KP 预览页已有的玩家卡数据整形逻辑，避免双份映射
- [ ] `B-1.3` 明确玩家端展示字段：名称、身份、简述或头像占位
- [ ] `B-1.4` 确认空状态时不渲染多余占位
- [ ] `B-1.5` 校验多玩家同时选中时的卡片布局和换行策略

产出物：

- 一个玩家端“在场玩家”展示区
- 一处共享或复用的数据整形逻辑
- 一组空状态 / 单人 / 多人展示截图或人工验证结论

涉及文件：

- [src/shared/stage/PlayerStage.tsx](/Users/yuhui/Documents/coc kp platform/src/shared/stage/PlayerStage.tsx:1)
- [src/shared/stage/KpStagePreview.tsx](/Users/yuhui/Documents/coc kp platform/src/shared/stage/KpStagePreview.tsx:1)
- [src/shared/stage/content.ts](/Users/yuhui/Documents/coc kp platform/src/shared/stage/content.ts:1)
- [src/shared/stage/stage.css](/Users/yuhui/Documents/coc kp platform/src/shared/stage/stage.css:1)

验收标准：

- 自定义模板房间中选中玩家席位后，玩家端可看到对应卡片
- 玩家端正文能看到玩家名称或席位信息

验证动作：

- [ ] 验证空状态
- [ ] 验证单个玩家席位
- [ ] 验证多个玩家席位同时在场
- [ ] 验证名称过长时不会挤坏布局

### B-2 KP 专用素材边界澄清

目标：KP 在选择素材时，能一眼知道哪些会同步给玩家，哪些只在主持侧可见。

拆分任务：

- [ ] `B-2.1` 区分“玩家可见素材”与“KP 专用素材”的数据来源
- [ ] `B-2.2` 为 KP 专用素材增加清晰标记
- [ ] `B-2.3` 在交互文案中明确“不会同步到玩家端”
- [ ] `B-2.4` 确认素材切换动作与展示语义一致
- [ ] `B-2.5` 复查默认模组与自定义模板两类素材是否都能正确标记

产出物：

- 一套清晰的素材分组或标签语义
- 一次 KP 端与玩家端对照验证

涉及文件：

- [src/pages/kp/App.tsx](/Users/yuhui/Documents/coc kp platform/src/pages/kp/App.tsx:1)
- [src/shared/stage/content.ts](/Users/yuhui/Documents/coc kp platform/src/shared/stage/content.ts:1)
- [src/shared/templates/index.ts](/Users/yuhui/Documents/coc kp platform/src/shared/templates/index.ts:1)

验收标准：

- KP 在素材区能明确知道当前内容是否会被玩家看到

验证动作：

- [ ] 选择一张公开素材并确认玩家端可见
- [ ] 选择一张 KP 专用素材并确认玩家端不可见
- [ ] 导入模板房间后再次确认标记不丢失

## 6. 阶段 C：移动端与信息层级修整

### C-1 房间状态徽章避让

目标：玩家端移动端首屏不再被状态徽章遮挡，且主文案优先展示。

拆分任务：

- [ ] `C-1.1` 调整移动端 `room-indicator` 布局位置
- [ ] `C-1.2` 处理 `room-indicator` 与 `stage-copy` 的遮挡关系
- [ ] `C-1.3` 复查 `390 x 844` 视口下首屏可读性
- [ ] `C-1.4` 复查窄屏 Android 常见宽度下的换行与溢出
- [ ] `C-1.5` 补一次移动端截图回归检查

产出物：

- 一版移动端安全布局
- 至少两种手机视口下的对照截图

涉及文件：

- [src/pages/player/player.css](/Users/yuhui/Documents/coc kp platform/src/pages/player/player.css:1)
- [src/shared/stage/stage.css](/Users/yuhui/Documents/coc kp platform/src/shared/stage/stage.css:1)

验收标准：

- `room-indicator` 不再遮挡玩家提示语
- 玩家端首屏主文案完整可读

验证动作：

- [ ] 验证 `390 x 844`
- [ ] 验证 `360 x 800`
- [ ] 验证长提示语场景下不会重新遮挡

### C-2 首屏信息优先级整理

目标：移动端首屏在不滚动的前提下，让标题、地点、提示语、连接状态一眼可读。

拆分任务：

- [ ] `C-2.1` 压缩顶部次级信息占位
- [ ] `C-2.2` 明确标题、地点、提示语、连接状态的视觉优先级
- [ ] `C-2.3` 为移动端首屏定义稳定布局
- [ ] `C-2.4` 复查地点标题过长、提示语过长时的折行策略

产出物：

- 一版稳定的移动端首屏层级规则
- 一次长文案场景验证

验收标准：

- 手机端首屏无需滚动即可快速读到标题、地点、提示语、连接状态

验证动作：

- [ ] 验证默认模组场景
- [ ] 验证自定义长标题场景
- [ ] 验证弱网连接状态文案出现时的布局稳定性

## 7. 阶段 D：自动化回归补齐

### D-1 内置模板同步 smoke 用例

目标：smoke test 能直接覆盖“默认模组房间已坏”的高风险回归。

拆分任务：

- [ ] `D-1.1` 为 `mist-blood-van` 建房流程增加 smoke case
- [ ] `D-1.2` 覆盖合法 `npcId` 推送
- [ ] `D-1.3` 覆盖合法 `playerIds` 推送
- [ ] `D-1.4` 断言接口返回 `200`
- [ ] `D-1.5` 断言状态回读正确
- [ ] `D-1.6` 在失败输出中打印出错字段，降低再次排查成本

产出物：

- 一条默认模组同步 smoke case
- 一组失败时可读的断言日志

涉及文件：

- [scripts/smoke.mjs](/Users/yuhui/Documents/coc kp platform/scripts/smoke.mjs:1)

验收标准：

- 内置模板同步异常时，smoke 直接失败

验证动作：

- [ ] 本地执行 `npm run smoke`
- [ ] 人工制造一次非法 `npcId` 或 `playerIds`，确认 smoke 会失败

### D-2 KP -> 玩家端最小联调用例

目标：至少有一条自动化检查真正覆盖“KP 推送 -> 玩家端消费”的最小业务闭环。

拆分任务：

- [ ] `D-2.1` 增加 KP 选中 NPC 后玩家端可见的回归检查
- [ ] `D-2.2` 增加 KP 选中玩家席位后玩家端可见的回归检查
- [ ] `D-2.3` 增加 KP 更新提示语后玩家端可见的回归检查
- [ ] `D-2.4` 明确断言是检查接口层、房间状态层还是玩家端渲染层
- [ ] `D-2.5` 保证失败时能指出断在“推送、存储、消费、渲染”哪一层

产出物：

- 一条覆盖完整链路的最小联动测试
- 一份失败定位信息更明确的断言输出

验收标准：

- 至少一条自动化用例覆盖“KP 推送 -> 玩家端消费”的闭环

验证动作：

- [ ] 至少执行一次完整自动化联调
- [ ] 确认 NPC、玩家席位、提示语三类更新里至少两类已被自动化覆盖

## 8. 推荐执行顺序

1. `A-1` + `A-2`
2. `A-3`
3. `B-1`
4. `C-1`
5. `B-2` + `C-2`
6. `D-1` + `D-2`

## 8.1 推荐提交切分

如果按低风险方式推进，建议最少切成 6 次提交：

1. `A-1` 模板解析统一
2. `A-2` 默认模组状态同步修复
3. `A-3` 错误提示兜底
4. `B-1` 玩家端在场玩家展示
5. `B-2` + `C-1` + `C-2` 体验与信息层级整理
6. `D-1` + `D-2` 自动化回归补齐

## 9. 完成定义

本清单完成时，至少应满足：

- 默认模组房间可正常同步地点 / NPC / 素材 / 玩家席位
- KP 页不再向用户暴露原始 JS 异常
- 玩家端可以看到在场玩家
- 玩家端移动端首屏无遮挡
- KP 能明确分辨公开素材与 KP 专用素材
- 自动化覆盖内置模板同步与最小联动闭环

## 10. 可视为关闭的判断标准

只有同时满足下面条件，才建议将本轮任务标记为完成：

- 所有 `P0` 与 `P1` 任务完成并验证
- 至少完成一轮默认模组房间的人工回归
- `npm run smoke` 通过
- 对玩家端完成至少一轮手机视口检查
